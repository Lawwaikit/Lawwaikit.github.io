# 2026-07-13

## 网站整体视觉风格重设计（去除 AI 生成痕迹）
- 背景：用户要求重设计 `Lawwaikit.github.io` 全部网页（HTML/CSS/JS），明确要去除"AI 生成痕迹"。
- 原风格指纹（典型的 AI 默认）：Tailwind 蓝 `#2563eb` 主色、纯白/浅灰背景、系统字体、`repeat(3,1fr)` 完全对称栅格、`0.2s ease` 通用淡入、柔和阴影 + `backdrop-filter` 玻璃导航。
- 新方向：**暖色"纸与墨"编辑风（paper & ink）**
  - 配色：奶油纸 `#F4EDE0` / 墨色 `#221E18` / 品牌锈红 `#B4431E` + 辅助松绿 `#2C5545`，无任何蓝/紫/渐变。
  - 字体：Fraunces（标题衬线）+ Figtree（正文拉丁）+ Space Mono（kicker/标签）+ Noto Serif SC（中文标题）。
  - 手法：risograph 硬边偏移阴影（非柔化）、卡片轻微旋转+错位、印章式序号、链接下划线擦除动画、纸面点阵纹理、虚线分隔。
- 改动文件：`css/style.css`（整体重写，保留所有被 HTML/JS 引用的类名）、7 个 HTML `<head>` 追加 Google Fonts 链接。
- 验证：类名交叉核对（HTML/JS 引用类全部有样式）、CSS 变量无悬空引用、花括号平衡（196/196）。已在 `127.0.0.1:8123` 起本地预览服务。
- 注意：本次 Ardot 画布适配器未连接，故直接在仓库文件上实现，未走画布流程。

## 导出设计令牌 + 生成多套主题方案
- 用户要求：把当前 Paper & Ink 设计语言导出为本地文件，并基于此生成 3-5 套不同风格主题方案，每套带预览 + 核心差异说明。
- 导出：`design-system/design-tokens.json`（颜色 light/dark 角色映射、字体家族+字阶、间距刻度、圆角、阴影、动效曲线、不规则参数）。
- 4 套替代主题（写入 `design-system/themes.json`）：
  1. Risograph Pop（网印流行：洋红+青，紧凑锐利，±1.2°）
  2. Brutalist Mono（粗野等宽：黑白+橙红单色，0 圆角，硬阴影）
  3. Quiet Luxury（静奢柔编辑：陶土+鼠尾草，全衬线，宽松 8-12px 圆角，发丝边框）
  4. Terminus Dark（终端暗色：琥珀金+青，近黑暖底，中等圆角，等宽偏重）
- 可视化：`design-system/theme-explorer.html`（自包含，嵌入数据，渲染当前基准 + 每套色板色块 + 实时示例组件 + 核心差异）。
- 应用方式：把对应主题的 `:root` / `[data-theme]` 变量块替换进 `css/style.css` 即可。
- 同样未走 Ardot 画布（适配器未连接），为文件型交付。

## 待办 / 观察
- 用户尚未看过实际效果，可能需要微调（如旋转角度、锈红浓度、字体加载性能）。
- `data/blog/index.js` 与 `js/blog.js` 未改动（仅视觉，未动内容/逻辑）。

## 用户反馈：之前 4 套主题"过于相似、缺乏差异化"
- 用户认为 Risograph Pop / Brutalist Mono / Quiet Luxury / Terminus Dark 仍属同一"暖色编辑"家族，区分度不足。
- 处理：① 先把已有产出快照存档到 `design-system/archive/2026-07-13-paper-and-ink/`（3 文件）；② 用**同一站点内容**渲染 4 套真正异质的视觉风格，逐一对比：
  1. `01-swiss.html` 国际主义（纯白+瑞士红、Inter、网格左对齐、发丝线、无圆角无阴影、编号 01/02/03）
  2. `02-brutalist.html` 新粗野主义（米黄底+黑粗边+硬偏移阴影、撞色填充、旋转贴纸、跑马灯、0 圆角）
  3. `03-clay.html` 有机柔感（暖奶油+陶土/鼠尾草、全圆角 30px+、黏土柔影、blob 背景、Quicksand/Nunito）
  4. `04-terminal.html` 终端暗色（近黑暖底+琥珀/青、等宽、网格点阵、$/>提示符）
- 对比枢纽：`design-system/styles/compare.html`（内嵌 4 个 iframe 逐一展示 + 设计特点说明）。
- 关键经验：用户要的"差异化"是**视觉家族层面的异质**（冷/raw/柔/暗），而非同一美学下的配色微调。后续做选项时应在配色+排版+布局+元素四个维度同时拉开。

## 对用户选定风格 03 的改造：05 冷调克制 Clay
- 用户选中 `03-clay.html`（有机柔感），要求：**保留布局/结构**，仅①收紧圆角（更精致专业、但不去圆角）②暖色→冷色（冷灰/蓝系/中性，降低色温，更冷静现代）③保持视觉层次与对比度不变。
- 产出 `design-system/styles/05-clay-cool.html`（独立完整页）+ 在 `compare.html` 追加为第 5 张卡片可对比。
- 具体改动（相对 03）：
  - 圆角：大面卡片/项目框 30px→14px；按钮 999px→12px；导航 999px→16px；导航链接 999px→10px；图标 18px→12px。微芯片（eyebrow/标签/状态徽章）保留 999px 药丸形作为刻意对比。
  - 配色（暖→冷，色温降）：`--bg #FBF5EF→#F1F4F8`（冷浅灰蓝）、`--ink #3D3A36→#2B3038`、`--mut #8A8378→#7C8493`、`--acc #E8916B→#3E6EA8`（钢蓝主色）、`--acc2 #8FB39B→#5E94A8`（雾青）、`--soft #F3E7DD→#E8EEF4`；blob 暖色→冷色 tint；第 3 图标 `#D9A85C→#7A8FB5`。
  - 阴影：模糊/扩散/层级**不变**，仅把暖色 tint 换成冷色 `rgba(70,95,130,...)`，保持原有层次与对比。
  - 字体/排版/间距/栅格/布局**完全沿用 03**，未动。
- 验证：05 与 compare 的 div 均平衡。

## 配色对比页：暖/冷各 4 组（圆角已锁定，仅换色）
- 用户确认 05 圆角设定合适（大面14/按钮12/导航16/导航链接10/图标12，芯片药丸），要求再生成多组暖色与冷色配色并排预览比较。
- 产出 `design-system/styles/06-palettes.html`：固定 05 圆角+结构+字体，仅替换配色令牌，8 套各含 6 色板 + 同一组件实时预览，分暖/冷两区。
  - 暖 W1 Terracotta&Sage / W2 Amber&Plum / W3 ClayRose&Olive / W4 Honey&Teal
  - 冷 C1 Steel&Mist(=05当前) / C2 Indigo&Periwinkle / C3 Teal&Slate / C4 Graphite&Ice
- 验证：div 137/137 平衡，8 个 .pal / 8 行色板 / 8 个组件齐全。

## 保存 8 套配色 + 将 C3 落地到真实站点
- 用户要求：①先保存当前 8 套配色（名称+色值）便于随时切换；②选冷色系 C3（Teal & Slate）整体应用到网站（背景/主色/辅色/文字/按钮链接/边框阴影等所有颜色 UI），确保统一冷色系氛围。
- ① 保存：`design-system/palettes.json`（8 套 W1-W4 暖、C1-C4 冷，每套含 bg/ink/mut/acc/acc2/soft/surface/blob1/blob2/ic3 + 阴影 rgba，附共享圆角与 surface 值）。
- ② 落地：重着色 `css/style.css` 的 `:root`(light) 与 `[data-theme="dark"]` 两块令牌 + 2 处硬编码 `#FBF7EF`(主按钮/移动端侧栏按钮文字)→`#F8FBFA`。**布局/字体/组件规则全部未动**（保留编辑风 risograph 硬阴影、错位卡片、点阵纸面等）。
  - 亮色 C3：paper `#EEF3F2` / ink `#283230` / mut `#6E847E` / acc `#2E8B86`(青碧) / acc2 `#5C7C84`(板岩) / soft `#E2EBE9` / line `#D2E1DD` / surface `#F8FBFA` / 高亮 `rgba(46,139,134,.30)` / code-bg `#E3ECEA` / nav-bg `rgba(238,243,242,.86)` / footer `#DCE7E4`。
  - 暗色 C3（同步改冷，避免切暗色回暖破坏统一）：paper `#15201E` / ink `#E4ECEA` / acc `#45A8A1` / acc2 `#7BA0A4` / line `#2E3C3A` 等。
- 验证：CSS 花括号 196/196 平衡；旧暖色值（`#F4EDE0/#B4431E/#2C5545/#FBF7EF` 等）全部清除；文件中所有 hex 均为冷色系（青/板岩/冷灰/冷白）。
- 预览：本地 8123 服务在 agent 子进程内 loopback 不可达（沙箱限制），改用直接打开 HTML 文件预览（index/about/projects/blog/contact）即可看到 C3 落地效果。
- 关键经验：用户把 `design-system/styles/` 下的 8 套 mockup 视作"网站当前使用的配色方案"，且偏好"配色方案=颜色令牌"的语义（改色不改结构）。后续切方案只需替换 `:root`/`[data-theme]` 变量块。

## 将 index.html 单页改为 Clay Cool 布局（对齐 05 + C3 配色）
- 用户要求：只改 `index.html`，让它的**布局/排版/组件/交互**对齐 `design-system/styles/05-clay-cool.html`，配色用 C3（Teal & Slate）；其它页面不动，完成后预览。
- 实现方式：**在 index.html 的 `<head>` 内插入一段内联 `<style>`，全部选择器作用域限定在 `body.clay` 下**，并给 `<body>` 加 `class="clay"`。因此共享的 `css/style.css` 与其它页面完全不受影响（关键约束：index 与所有页共用 style.css，不能改 style.css）。
- 同时为 index 单独加载 Quicksand/Nunito/DM Mono 字体链接（原 Fraunces 等保留，无害）。
- Clay 视觉：奶白冷底 `#EEF3F2` + 固定柔化 blob（body::before/::after，替换点阵纸面）；白色圆角卡片（大面 14px/按钮 12px/导航 16px/导航链接 10px/图标 12px，芯片药丸）；Quicksand 标题+brand+按钮、Nunito 正文、DM Mono 用于 eyebrow/序号方块/状态/标签/blog-meta；卡片 hover translateY(-6px)、按钮 hover translateY(-3px)。
- 映射 index 真实类名（与 05 类名不同）：site-nav/nav-brand/nav-links/hero.profile-hero/hero-inner/eyebrow/subtitle/hero-tags/hero-buttons/btn.btn-primary,.btn-secondary/section-header h2/feature-grid.feature-card.card-kicker(做成 54px 彩色圆角方块，nth-child 用 acc/acc2/ic3)/project-grid.project-card.status(.status-live)/card-tags.tag/text-link/blog-list.blog-item(.blog-draft)/footer。
- 特意处理：用 `content:none!important` 抑制 base "paper & ink" 的装饰伪元素（hero 侧注、印章、下划线擦除 ::after）；nav 深选择器加 `.site-nav` 前缀以压过 base 的 `.site-nav .nav-links a`(0,3,0) 特异性；移动端 <=760 自写汉堡菜单下拉（.nav-links.active 显示）。
- 附带深色模式：`html[data-theme="dark"] body.clay` 覆盖 clay 变量（深青底 `#15201E`/亮青 acc `#48ABA4`），使主题切换在 Clay 皮肤下也协调（05 本身只有亮色）。
- 验证：内联 style 花括号 83/83 平衡；`body.clay` 规则 105 处；C3 acc `#2E8B86`/bg `#EEF3F2` 均在。

## 将 Clay Cool（05+C3）从 index 推广到全站其余 6 页
- 用户确认 index 的 Clay Cool 样式"基本没问题"，要求**应用到其他页面文件**（about/projects/blog/contact/post/post-intro），保持 C3 配色与 05 视觉语言统一。
- 重构：把 index 内联 `<style>` 抽成独立 `css/clay-cool.css`（整文件限定 `body.clay` 作用域），并**扩展覆盖全站组件**：导航/hero/page-header/按钮/sections、feature-card/card-kicker、skill-item/skill-icon(about)、profile-summary/avatar(about)、timeline(about)、project-card/status/tags/project-meta、blog-list(首页)、contact-link/contact-icon(contact)、blog-layout/sidebar/blog-tree/blog-toc/sidebar-toggle(blog/post)、post-content/post-content-rendered(文章正文，code/quote/table)、back-link、footer；并中和原 Paper&Ink 的硬阴影/错位旋转(3n 倾斜用 !important 清零)/点阵纸面/下划线擦除/EST 竖排边注等痕迹。
- 机制：所有 7 个页面 `<body class="clay">` + 引入 Quicksand/Nunito/DM Mono 字体链接 + 引入 `css/clay-cool.css`（放在 `css/style.css` 之后以保证同特异性下顺序胜出）；index 改为外链（删内联）。
- 关键特异性处理：`main:not(.blog-main)` 约束常规页容器宽度，但**排除** `.blog-main` 两栏布局不受影响；feature/skill 旋转用 `transform:none!important` 清零；nav/link hover 用 `body.clay` 前缀(0,3,0)压过 base(0,2,0)。
- 验证：clay-cool.css 花括号 155/155；7 页均 `body.clay=True`/`clayLink=True`/`inlineStyle=False`；index 仍引用 style.css+字体+clay-cool；临时脚本已清理。

## 内容改造第一步：去按钮 + 博客留白 + 内容策略方案
- 用户诉求：①**暂时去掉**中英文切换 + 日间/夜间模式按钮（想专注内容）；②博客页展示未定，**保留顶部博客按钮、博客页改为空白内容**；③然后分析个人站还适合展示什么内容/现有内容如何改造，出方案供预览选择；**保留现有网页样式**（Clay Cool）。
- ① 去按钮：用脚本从全部 7 个 HTML 的 `<nav>` 中删除 `<li class="nav-actions">`（含 `#langToggle` + `#themeToggle`）。`js/main.js` 原已用 `if(themeToggle)/if(langToggle)` 守卫，删除无报错。另在 `js/main.js` 把 `savedTheme/savedLang` 改为硬编码 `'light'`/`'zh'`，保证无按钮时站点恒定"中文 + 日间"，避免旧 localStorage 偏好导致意外英文/暗色（可恢复）。
- ② 博客留白：`blog.html` 删除 `blog-layout`/`blog-sidebar`/`sidebar-toggle` 及 `data/blog/index.js`+`marked`+`js/blog.js`+内联 `Blog.renderTree` 调用，改为一个空 `<main class="page">`（page-header「博客」+ muted 占位「内容筹备中，敬请期待」），保留顶部博客按钮 + footer + `js/main.js`。博客动态逻辑文件未删，恢复时只改回 blog.html 即可。
- ③ 内容策略预览页：`design-system/styles/07-content-plans.html`（沿用站点 Clay Cool：`body.clay`+`css/clay-cool.css`）。内容：现状诊断 4 短板 → 通用内容清单 6 项（一句定位/头像/3-4 真实项目/真实时间线/多渠道联系/Now 板块）→ 4 套内容架构方案（A 现状精修 / B 作品集优先 / C 写作知识花园 / D 单页个人站），每套含迷你线框 mock + 要点 + 适合谁 → 横向对比表 → 建议（短期走 A，想一鼓作气走 D）。
- 验证：7 页 nav-actions/langToggle/themeToggle 均 GONE；blog.html 无 blog-layout/blog.js/marked、有 page-header「博客」且仍引 main.js；main.js 两处硬编码生效；07 页 html/head/body/style/table/div 均平衡（div 47/47）、4 张 option-card、引用 clay-cool.css+body.clay。
- 下一步（待用户选方向）：按选定方案重写对应页面内容与结构，仍用现有 Clay Cool 样式，并给每页出"内容填写模板"。

## 内容架构方案可视化预览（08，替代纯文字 07）
- 用户重新要求"分析个人站还适合展示什么内容 / 现有内容如何改造，出设计方案预览选择"，且隐含对上一版 07（文字+ASCII 线框）不够直观的不满。
- 产出 `design-system/styles/08-content-designs.html`：**自包含 Clay Cool（C3）页面**，每个方案是**真实渲染的首页 mockup**（非线框），填入真实感样例文案（非"待补充"），便于直观对比。
  - 顶部：现状速览（5 页内容盘点 + 3 短板：内容单薄/身份模糊[Hero 写产品思维·AI·写作 vs 技能写测试/后端/AI/写作，自相矛盾]/展示无重点）。
  - 4 套方案（各含渲染 mockup + 说明 + 适合谁）：A 作品集优先(case-study) / B 写作知识花园(主题索引) / C 单页滚动(推荐起步,加 Now) / D 完整分站(加 Uses/Now/Resume)。
  - 横向对比表（内容重心/页数/维护成本/新增板块/适合/落地风险）。
  - 建议：短期走 C 单页（最稳最省、补齐"无重点"短板）；中期 A 的 case-study；长期 D；博客先留空将来接 B。
  - **核心第一步**：先定身份定位（建议"测试开发 / AI 工具实践"，与技能页统一），补 2-3 真实项目。
- 关键：预览页独立、未改站点任何文件；沿用 C3 配色/圆角/Quicksand·Nunito·DM Mono，与现有站点视觉一致。
- 验证：div 143/143、span 46/46 平衡；4 个 .mk mockup；C3 色 `#EEF3F2`/`#2E8B86` 在用。

## 提交推送（最后一次动作，被中断）
- 用户最后指令「帮我提交推送」。我先 `git status`/`branch`/`log` 触发 safe.directory 报错 → `git config --global --add safe.directory` 修复 → 重查 status（**此时被用户打断**），未执行 add/commit/push。
- 实测待提交范围：9 个已修改（about/blog/contact/css/style.css/index/js/main.js/post-intro/post/projects）+ 3 组未跟踪（.workbuddy/、css/clay-cool.css、design-system/）。分支 main，上次提交 `8b348b3`。
- 产出 `design-system/COMMIT-PLAN.md`：总结最后一次动作 + 当前 git 状态 + 分步提交计划（含建议 commit message，排除 .workbuddy/）+ 待确认项。**未执行任何 git 提交/推送**。
- 用户澄清：「最后一次」指**网页内容架构方案（08）**，不是提交推送；要求忽略提交计划，重新生成 md。已 `rm design-system/COMMIT-PLAN.md`，改出 `design-system/CONTENT-PLAN.md`：背景 + 现状诊断（5 页饱和度 + 3 短板：单薄/身份模糊/Hero 与技能页矛盾/无重点）+ 4 套方案（A 作品集优先 / B 写作花园 / C 单页滚动★推荐 / D 完整分站）+ 横向对比表 + 分期建议（短 C→中 A→长 D）+ 按方案的执行计划 + 待确认项。**未改站点文件**。
